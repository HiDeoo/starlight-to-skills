---
title: Skill Callout
description: Learn how to prominently recommend an agent skill and show how to install it.
---

import SkillCallout from 'starlight-to-skills/components/SkillCallout.astro'

To prominently recommend an agent skill and show how to install it, use the `<SkillCallout>` component.

import { Preview } from '@hideoo/starlight-plugins-docs-components'

<Preview>

<Fragment slot="preview">
  <SkillCallout skill="starlight-to-skills-install">

    Install this skill and ask your agent to add Starlight to Skills to your project.

  </SkillCallout>
</Fragment>

</Preview>

The component requires Astro's [`site` option](https://docs.astro.build/en/reference/configuration-reference/#site) to be set to your site's deployed URL.

## Import

```tsx
import SkillCallout from 'starlight-to-skills/components/SkillCallout.astro'
```

## Usage

Recommend a skill prominently using the `<SkillCallout>` component.
A `<SkillCallout>` requires a [`skill`](#skill) prop matching the [name](/skill-definition/) of an approved skill.

<Preview>

```mdx
import SkillCallout from 'starlight-to-skills/components/SkillCallout.astro'

<SkillCallout skill="starlight-to-skills-install" />
```

<Fragment slot="preview">

<SkillCallout skill="starlight-to-skills-install" />

</Fragment>

</Preview>

### Add a description

Add a description to provide additional context or instructions.

<Preview>

```mdx {5-6}
import SkillCallout from 'starlight-to-skills/components/SkillCallout.astro'

<SkillCallout skill="starlight-to-skills-install">

We have a skill specifically designed to help you install Starlight to Skills.
Install it before asking your agent to add and configure the plugin.

</SkillCallout>
```

<Fragment slot="preview">

<SkillCallout skill="starlight-to-skills-install">

We have a skill specifically designed to help you install Starlight to Skills.
Install it before asking your agent to add and configure the plugin.

</SkillCallout>

</Fragment>

</Preview>

### Use custom titles

Override the default title by using the `title` attribue.

<Preview>

{/* prettier-ignore */}
```mdx /title=.*"/
import SkillCallout from 'starlight-to-skills/components/SkillCallout.astro'

<SkillCallout
  skill="starlight-to-skills-install"
  title="Use your agent to install Starlight to Skills"
/>
```

<Fragment slot="preview">

<SkillCallout skill="starlight-to-skills-install" title="Use your agent to install Starlight to Skills" />

</Fragment>

</Preview>

### Add follow-up instructions

Add follow-up instructions displayed below the installation command by using the `footer` slot.

<Preview>

{/* prettier-ignore */}
```mdx {7-9}
import SkillCallout from 'starlight-to-skills/components/SkillCallout.astro'

<SkillCallout skill="starlight-to-skills-install">

Install the skill specifically designed to help you add Starlight to Skills to your project.

<Fragment slot="footer">
  Then ask your agent to add and configure the plugin.
</Fragment>

</SkillCallout>
```

<Fragment slot="preview">

<SkillCallout skill="starlight-to-skills-install">

Install the skill specifically designed to help you add Starlight to Skills to your project.

<Fragment slot="footer">Then ask your agent to add and configure the plugin.</Fragment>

</SkillCallout>

</Fragment>

</Preview>

## `<SkillCallout>` Props

The `<SkillCallout>` component accepts the following prop:

### `skill`

**Required**  
**Type:** `string`

The [name](/skill-definition/) of the skill to recommend.
